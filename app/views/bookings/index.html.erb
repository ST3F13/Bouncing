<p id="notice"><%= notice %></p>
<div class="bookings-container-box">
  <h1>All my bookings 🐐</h1>

<div class="cards">

<% @bookings.each do |booking| %>>

 <div class="card">
                    <div class="card-image">
                      <%= cl_image_tag booking.goat_ad.medias.first.key, height: 200, width: 200, crop: :fill%>
                    </div><br/>
                  <div class="card-info">
                    <h4><%= booking.name %></h4>
                    <h5><%= booking.goat_ad.name %></h5>
                    <p><%= booking.start_time %></p>
                    <p><%= booking.end_time %></p>
                    <%# <td><%= link_to booking_path(booking) do %>
                    <%# <i class="far fa-eye"></i> %>
                    <%# <% end %></td>
                    <td><%= link_to edit_booking_path(booking) do %>
                    <i class="far fa-edit"></i>
                    <% end %></td>
                    <%# <td><%= link_to booking_path(booking) method: :delete, data: { confirm: 'Are you sure?' } %>
                    <%# <i class="far fa-edit"></i> %>
                    <%# <% end %></td>
                  </div>
                </div>
    <% end %>
    <br>
  </div>
</div>

<%# <%= link_to 'New Booking', new_booking_path %>
